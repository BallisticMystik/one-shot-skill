interface:
  display_name: "One Shot"
  short_description: "Governed one-shot execution"
  default_prompt: "Use the one-shot skill to deeply prepare this TimeMachineV9 sidechain, lock the plan, then execute autonomously while enforcing hard-stop approvals, checkpoints, anti-drift checks, and frontend styleguide rules."
